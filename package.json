{
  "name": "dsh-memory-core",
  "version": "0.2.0",
  "description": "Portable routing, audit, and regression primitives for layered DSH memory.",
  "type": "module",
  "bin": { "dsh-memory": "./bin/dsh-memory.mjs", "dsh-memory-evolution": "./bin/dsh-memory-evolution.mjs" },
  "scripts": {
    "check": "node --check src/index.mjs && node --check src/evolution.mjs && node --check bin/dsh-memory.mjs && node --check bin/dsh-memory-evolution.mjs",
    "test": "node --test",
    "audit": "node ./bin/dsh-memory.mjs audit --home ./fixtures/sample-home --strict"
  },
  "engines": { "node": ">=20" },
  "license": "MIT"
}
